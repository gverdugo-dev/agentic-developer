---
name: adev-plugin-builder
description: "Opinionated guide for building a well-formed plugin scaffolded by adev — plugin.json, the commands/agents/skills/hooks layout and conventions. Use when the user is writing or filling in a plugin created with `adev new plugin`, or asks how a plugin should be structured."
---

# adev-plugin-builder

TODO: the opinionated plugin-development workflow.

Cover: the .claude-plugin/plugin.json manifest, the commands/, agents/, skills/
and hooks/ subfolders, naming conventions, and how to go from an
`adev new plugin` scaffold to a finished, installable plugin.
