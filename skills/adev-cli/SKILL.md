---
name: adev-cli
description: "How to drive the adev CLI to scaffold and remove AI coding-agent artifacts. Use whenever the user wants to create or delete a skill, plugin or plugin-marketplace with adev, asks how adev works, or mentions 'adev', 'scaffold a skill/plugin', or 'agentic-dev'."
---

# adev-cli

TODO: document the adev command surface so the agent can drive it.

Cover: `adev new|delete <artifact> <name> [scope] [harness] [--force]`,
`adev setup [harness]`, the artifact types, scope (project vs local), harness
auto-detection, and the `--force` overwrite behavior.
